{% extends './base.html.twig' %}

{% block body %}
    <p class="text-muted" style="text-align: center;"><a href="/admin">Retour à la page d'administration</a></p>

    <div class="container">
        <div class="row">
            {% for article in articles %}
            <div class="card" style="margin: 20px;">
                <div class="card-body">
                    <h5 class="card-title">{{ article.titre }}</h5>
                    <p class="card-text">{{ article.chapo }}</p>
                    <a href="/update/article/{{ article.id }}" class="btn btn-info">Editer l'article</a>
                    <a href="/delete/article/{{ article.id }}/{{ token }}" class="btn btn-danger">Supprimer l'article</a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}

{% block footer %}{% endblock %}


